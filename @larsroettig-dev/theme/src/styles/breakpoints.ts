import { tailwind } from '@theme-ui/presets';

export const breakpointSm = `@media (min-width: ${tailwind.breakpoints[1]})`;
export const breakpointMd = `@media (min-width: ${tailwind.breakpoints[2]})`;
export const breakpointLg = `@media (min-width: ${tailwind.breakpoints[3]})`;
